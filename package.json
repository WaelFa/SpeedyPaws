{
  "name": "speedypaws",
  "version": "1.0.0",
  "description": "🐾 Cute YouTube playback speed controller with smart features",
  "type": "module",
  "scripts": {
    "dev": "node scripts/vite-build.js && node scripts/build.js",
    "build": "node scripts/vite-build.js && node scripts/build.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "typescript": "^5.3.3",
    "vite": "^5.0.10"
  },
  "author": "SpeedyPaws",
  "license": "MIT"
}
